# Copy to .env.local (gitignored) and fill in real values.
# The Spring Boot app reads these from the environment — never commit secrets.
DATABASE_URL=jdbc:postgresql://localhost:5432/stockpile_3d
DB_USER=postgres
DB_PASSWORD=changeme
